import AuthLayouts from "../components/Layouts/AuthLayouts";

const RegisterPage = () => {
  return (
    <div className="bg-background min-h-screen border">
      <AuthLayouts title="register"></AuthLayouts>
    </div>
  );
};

export default RegisterPage;
